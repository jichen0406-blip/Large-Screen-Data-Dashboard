# 敏感数据 - 绝不提交
rawdata/
# 依赖/临时文件
node_modules/
.DS_Store
*.log
# 模板遗留未引用的文件（如确认不需要可删除）
js/44.json
js/area_echarts.js
js/Bubble.js

# 异常订单 AI 摘要本地缓存（含生成结果，不入库）
.abn_ai_cache.json

# DeepSeek API 密钥（本地文件，不入库）
deepseek_api_key.txt
# AI 摘要缓存（含异常订单管理 + 省份&医院数据，不入库）
.ai_summary_cache.json
