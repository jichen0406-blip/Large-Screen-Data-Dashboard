# 敏感数据 - 绝不提交
rawdata/
# 依赖/临时文件
node_modules/
.DS_Store
*.log
# 导出/交付文件（本地生成，不进 git）
*.xlsx
# 模板遗留未引用的文件（如确认不需要可删除）
js/44.json
js/area_echarts.js
js/Bubble.js

sync_server.js
users.js.bak
# 部署前备份目录（不进 git）
_backup*/
_backup*
