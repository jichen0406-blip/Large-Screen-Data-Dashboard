# 驯鹿生物大数据监控平台 — 项目文档

> 目录：`D:\VS Code\fucaso-board`
> 部署仓库：https://github.com/jichen0406-blip/Large-Screen-Data-Dashboard
> 技术栈：HTML + CSS + jQuery + ECharts 4.0.5（中国地图 geo）+ Node.js（数据生成）

---

## 1. 项目概述

数据驱动的大屏看板（双页面）。**总览页**（浏览器标签"福可苏业绩总览"）展示驯鹿生物的订单 / 回输 / 单采 / 放行等业务指标；**Page2** 展示海外患者 / 海外商业化（导流、NPP、商业化分类）。数据来自 Excel（bs_order + masterdata + 异常订单 + order dict），由 `build_data.js` 每日生成静态 `js/data.js`（全局 `BOARD_DATA`，含 `P2` 子数据），前端直接引用，无需后端。

页面访问需要密码门禁（客户端校验，密码 `fucaso2026`）。

---

## 2. 目录结构

```
fucaso-board/
├── index.html            # 主页面（总览）：登录门禁 + 表头 + 第一排 + 第二排图表
├── page2.html            # Page2：年份切换 + 4卡片 + 2世界地图 + 明细表 + Top10浮窗
├── css/style.css         # 全部样式
├── build_data.js         # 数据管线：Excel → js/data.js（含 P2 海外/商业化数据）
├── build_geo.js          # 打包 34 省 GeoJSON → js/geo-data.js（内联，离线兜底）
├── deploy.sh             # 一键部署：生成数据 + 提交 + 推送到 GitHub
├── package.json          # npm run data → node build_data.js
├── .gitignore            # 排除 rawdata/node_modules/遗留死文件
├── images/               # 背景图、logo（logo-white.png 为白字版）
└── js/
    ├── jquery.js / echarts.min.js / china.js   # 第三方库
    ├── data.js           # 【生成文件】BOARD_DATA，勿手改
    ├── geo-data.js       # 【生成文件】34 省 GeoJSON 内联（离线/file:// 兜底）
    ├── world-data.js     # 【生成文件】世界 GeoJSON 内联（含台湾/香港/澳门独立区域）
    ├── js.js             # 总览页主逻辑：滚动、月度趋势、异常订单、COE、Top30、当月时间轴
    ├── map.js            # 总览页地图：散点、自动轮播、省份浮窗
    ├── page2.js          # Page2 逻辑：年份切换、卡片、两世界地图、明细表、Top10
    └── geo/              # 省份(34) + 世界(world.json) GeoJSON（源文件）
```

---

## 3. 数据逻辑（build_data.js → js/data.js）

### 3.1 数据源

| 文件 | 位置 | 用途 |
|---|---|---|
| `bs_order*.xlsx` | `D:\VS Code\fucaso-dashboard\rawdata\` | 订单主数据 |
| `masterdata.xlsx` | 同上 | 医院主数据（标准名称/省份/城市/COE 标记） |
| `异常订单.xlsx` | 同上 | 月度异常订单（PBMC分选失败/第一次/二次生产失败） |
| `order dict.xlsx` | 同上 | 订单分类标记（导流方式 / 海外导流国家 / 合同号） |

脚本优先读 `fucaso-board/rawdata`，不存在则回退到 `fucaso-dashboard/rawdata`。

### 3.2 医院匹配规则

- 优先 `医疗机构编码`（第 33 列）查 masterMap；
- 为空则用 `处方来源医疗机构编号`（第 6 列）；
- 均无 → 记录到 `unmatched`，**脚本报错退出**（质量门禁，需补 masterdata）；
- 排除新加坡；未匹配医院记为 `未知医院`。

### 3.3 关键口径（与 fucaso-dashboard/build_poster.js 一致）

- **YTD**：当年 1/1 ~ 今天；**MTD**：当月 1 日 ~ 今天；
- **同比**：去年同截止日（`去年 + 当年月日`），去年为 0 → 返回 null，前端显示 `--`；
- 数据截止日 `DP` = 当天日期。

### 3.4 BOARD_DATA 字段

| 字段 | 内容 |
|---|---|
| `UPDATED` | bs_order 文件修改时间（表头"数据更新时间"） |
| `DP` / `Y` | 数据截止日 / 年份 |
| `YTD{O,R,A,Q}` / `MTD{O,R,A,Q}` | 下单/回输/单采/放行，全年 / 当月 |
| `MONTH{O,R,OLY,RLY}` | 当年+去年各 12 个月下单/回输（月度趋势双折线） |
| `PROV` / `CITY` | 省份 / 城市 → {o,r,a,q} |
| `TOP_O` / `TOP_R` | Top30 下单 / 回输医院（含同比） |
| `LAST7` | **当月每天**（1 号~今天）下单/回输医院明细（时间轴） |
| `HOSP_PROV{O,R}` | 各省 Top5 下单/回输医院（省份浮窗右侧榜单） |
| `COE{O,R}` | SCOE/COE/RCOE/Others 四类销量（COE 占比饼图） |
| `ABN` | 当年异常订单：total/pbmc/first/second |
| `CITY_PROV` | 城市 → 省份（大地图轮播按省份分组） |
| `P2` | Page2 数据：`YEARS` 年份 / `CARD` 卡片 / `LEFT` 海外患者 / `RIGHT` 海外商业化 / `RIGHTD` 国家×导流分类 / `TOP10` 医院 |

---

## 4. 页面结构与功能

### 4.1 表头
- **logo**（`images/logo-white.png`，驯鹿生物+iaso 白字版）+ 标题"驯鹿生物大数据监控平台"居中；浏览器标签标题为"福可苏业绩总览"；
- 左侧：**数据更新时间**（`B.UPDATED`）+ **"☰ 网页导航"按钮**（弹菜单：总览 / Page2 / 预留，可设默认首页）；
- 右侧：**时钟**（内联脚本每秒刷新）+ **上海天气**（Open-Meteo 实时接口）。

### 4.2 第一排（22% / 56% / 22%）
- **左**：Top30 下单 / 回输医院表格（表头 sticky，内容**自动无缝滚动**，悬停暂停）；
- **中**：中国地图
  - YTD / MTD 各 4 个卡片（左右两侧，`space-evenly` 均布）；
  - 雷达扫描动画（`.radar-sweep` conic-gradient 旋转）；
  - 城市散点 + **自动轮播**：按省份北→南、省内纬度排序，逐城市高亮展示卡片（5s 一个）；**登录后才启动**（未登录不轮播，登录成功后由登录脚本触发）；
  - **点击省份 或 城市散点** → 浮窗显示该省/市地图 + 城市散点 + 右侧 Top5 下单/回输医院榜单；地图数据在线走 fetch 按需加载，离线（file://）自动懒加载内联 `js/geo-data.js`；
- **右**：**当月**每天下单/回输时间轴（自动滚动，表头 sticky）。

### 4.3 第二排（4 个 25%）
- **下单月度趋势** / **回输月度趋势**：双折线——当年：下单蓝 / 回输绿，去年统一亮灰；折线上方逐月同比（绿↑/红↓/灰--，0 位小数，无数据月不显示）；
- **YTD异常订单**：左侧红色大数字（当年合计，随屏缩放 `clamp`）+ 右侧三类失败柱状图（黄/橙/红）；整行高度随视口缩放（`.nav2>li` `clamp(200px, calc(100vh - 720px), 380px)`），柱宽百分比、yAxis 头部余量，标签不裁切；
- **COE医院销量占比**：两个环形饼图（下单 / 回输），中心标题，底部图例，扇区内显示占比，四类配色 SCOE 绿 / COE 蓝 / RCOE 青 / Others 黄。

### 4.4 Page2（海外患者 / 海外商业化）
- 表头同总览页（浏览器标签"福可苏业绩总览 · Page 2"）；登录 sessionStorage 共享，总览登录过则免密；
- **年份切换**（第一行第 5 个容器，横向平铺整页）：全部 + 数据实际年份（2027 等自动出现）；**口径与 page1 统一**——下单按下单日期年、回输按回输结束时间年；
- **4 张卡片**（占 4/5 页宽，间距自适应）：NPP/商业化 下单/回输、国内导流(医生导流+OB导流) 下单/回输（导流列为空的国内直接患者不计入）；
- **左图 海外患者（含导流以及海外商业化）**：热力色阶 + 各国飞向中国的飞线 + 中国中心辐射光圈"中国驯鹿"；**不轮播，鼠标移入区域展示下单/回输卡片**；
- **右图 海外商业化**：仅 NPP/商业化，**圆点向外辐射**（大小随销量）；自动轮播（5s）+ 点击区域弹出 Top10 医院浮窗（左下单/右回输）；
- **地图下方明细表**：大洲合并同类（rowspan）、先按大洲合计下单降序、洲内按国家下单降序、固定 10 行补空白、表头锁死；左表自动滚动（80ms/1px，悬停暂停）；右表多一列"导流分类"（医生导流/OB导流/NPP/商业化）；
- **地图放大全屏**：每张地图右上角 ⛶，容器移入全屏层并 `resize()`，交互保留；点 × / 空白 / Esc 恢复；
- **网页导航菜单**：总览 / Page2 / 预留；可设置"默认首页"（localStorage），从站点外部首次进入自动跳转。

---

## 5. 代码要点

### 5.1 js.js
| 函数 | 作用 |
|---|---|
| `countUp(el,target)` | 数字滚动动画（easeOutCubic，1.2s） |
| `setupTblScroll / setupSevScroll` | 无缝滚动：复制内容一份，`scrollTop` 到达一份高度即回绕（setInterval 80ms/1px，悬停暂停） |
| `monthlyTrend()` | 渲染 echart6 / echart4 双折线（下单当年蓝、回输当年绿，颜色参数可配） |
| `echarts_31()` | 渲染 fb03/fb04 COE 饼图 + myd1 异常订单柱状图（柱宽百分比、yAxis 余量防标签裁切） |
| `board_ext()` | MTD 数字、异常订单总数、更新时间、天气、Top30 表格、当月时间轴 |

### 5.2 map.js
- `playOrder` 构建：`B.CITY_PROV` 按省分组 → 省份按平均纬度降序 → 省内城市按纬度降序 → `showNextCity` 顺序轮播；轮播受登录门控（`window.startBoardAuto`，登录脚本成功校验后调用）；
- 点击交互：点省份边界 → `openProvince(省份)`；点城市散点 → 用 `fullToKey`（省全名→短名）反查并打开所在省（解决直辖市/小省圆点盖住区域点不到的问题）；
- `openProvince` 加载地图：在线优先 `fetch('js/geo/{adcode}.json' or '{adcode}_full.json')`（按需快）；失败则懒加载 `js/geo-data.js` 内联数据（file:// 离线场景）；
- 直辖市（北京/天津/上海/重庆/港/澳/台）用 `{adcode}.json`（自身边界），普通省用 `_full.json`（城市边界）；
- 浮窗打开时 `panelOpen=true` 停止轮播，关闭恢复；geo 组件 `silent:true` 避免红字标签。

### 5.3 page2.js
| 函数 | 作用 |
|---|---|
| `countUp2` | 卡片数字滚动（与 page1 countUp 同款，Page2 独立实现） |
| `mapOption(md, withLines, radiate)` | 共用 option 构造：左图(热力+飞线+中国标记) / 右图(辐射圆点) |
| `startAuto / chartPause` | 右图轮播（按纬度北→南，5s 一个）与暂停 |
| `fillTbl / fillTblRight` | 明细表渲染：大洲合并、按下单排序、固定 10 行补空白 |
| `setupTblAutoScroll` | 左表自动滚动（复制 tbody、80ms/1px、悬停暂停） |
| `openTop10` | 右图点击 → Top10 医院浮窗 |

---

## 6. 接口与配置

### 6.1 天气接口（实时，需外网）
```
https://api.open-meteo.com/v1/forecast?latitude=31.23&longitude=121.47&current=temperature_2m,weather_code&timezone=Asia%2FShanghai
```
- 前端 fetch，无 key、支持跨域；失败显示"上海 --"；
- 天气码映射为符号：☀晴 / ⛅多云 / ☁阴 / ☔雨(☔☔中雨 / ☔☔☔大雨) / ❄雪 / ⛈雷阵雨。

### 6.2 省份地图（本地 + 内联兜底）
- `js/geo/` 预下载 34 个省份 GeoJSON（来源：阿里 DataV GeoAtlas）；`build_geo.js` 可打包为 `js/geo-data.js`（内联 `window.GEO_DATA`）；
- 加载策略：在线（GitHub Pages / 本地服务）优先 fetch 单文件按需加载（快）；`file://` 双击打开时浏览器屏蔽 fetch → 自动懒加载 `js/geo-data.js` 内联数据，无需外网/服务；
- 台湾无 `_full` 数据，特判用 `710000.json`。

### 6.5 世界地图（本地内联）
- `js/geo/world.json`（echarts 4.9.0 世界地图源）→ 生成 `js/world-data.js` 内联（`window.WORLD_GEO`，末尾追加台湾/香港/澳门独立区域）；
- 页面加载时把英文区域名改中文、补 `cp` 标签锚点（**最大外环坐标平均中心**，避免新西兰等多岛/跨反经线国家标签漂到海上）后 `registerMap('world')`；
- 内联脚本标签加载，file:// 双击离线可用。

### 6.3 登录门禁
- 密码 `fucaso2026`，以 SHA-256 哈希 `fcde78c0a777ada04fec4ed613421feb59ee71e70608fa7bf53c386970395c66` 存储于 `index.html` 内联脚本；
- 同浏览器会话（sessionStorage）刷新免登录；
- 登录遮罩 `.login-mask` 用"实色底 + 双渐变"多层兜底，输入密码时完全遮住后面数据；
- 地图轮播在登录前不启动：map.js 加载时检查 `sessionStorage.board_authed`，未登录仅暴露 `window.startBoardAuto`；密码校验成功后由登录脚本调用启动；
- 注意：**纯前端门禁，非真正安全**（源码可见校验逻辑）。

### 6.4 部署（GitHub）
- 远程：`git@github.com:jichen0406-blip/Large-Screen-Data-Dashboard.git`（SSH 认证，分支 main）；
- 提交身份：`jichen0406-blip` + noreply 邮箱（在 `.git/config`）；
- 一键命令：`cd "D:/VS Code/fucaso-board" && bash deploy.sh`。

---

## 7. 日常操作

### 7.1 更新数据 + 部署
1. 把最新 `bs_order*.xlsx`、`masterdata.xlsx`、`异常订单.xlsx` 放入 `D:\VS Code\fucaso-dashboard\rawdata\`，**先关闭 Excel**；
2. 运行：`bash deploy.sh`（自动：node build_data.js → node build_geo.js → git commit → push）；
3. 浏览器 **Ctrl+F5 强刷**查看。

### 7.2 修改密码
1. `node -e "console.log(require('crypto').createHash('sha256').update('新密码').digest('hex'))"` 得到新哈希；
2. 替换 `index.html` 内联脚本中的 `HASH` 值。

### 7.3 本地预览
```
cd "D:/VS Code/fucaso-board" && npx serve -p 53202
```

---

## 8. 实施经验 / 踩坑记录

1. **ECharts 4.0.5 的 geo 强调态语法**：省份地图红色城市名问题——v5 的顶层 `emphasis:{label:...}` 在 v4 不生效，必须用 v4 嵌套写法 `label:{normal:{...}, emphasis:{show:false}}`；彻底去掉区域交互则设 `geo.silent:true`。
2. **无缝滚动的"不滚动"坑**：rAF + 亚像素（`scrollTop += dt/240`）在某些浏览器不生效；改用 `setInterval(80ms) + 整数 1px` 步长最可靠；内容复制一份后按"一份高度"回绕实现无缝。
3. **CSS 优先级**：`.wancheng span` 会把数字字号强制成 14px，覆盖 h3 继承——子元素被更通用规则命中时，需给目标元素单独加更高优先级规则（`.wancheng h3.abn-num #abnNum`）。
4. **高度溢出**：容器 `height:100%` 会把标题（tit01）高度也算进去导致图表被底部裁切，需 `height:calc(100% - 40px)` 修正。
5. **中文路径编码**：Windows + Node 直接 `require` 含中文路径的 xlsx 可能 ENOENT，用 `path.join` + 脚本文件（UTF-8）而非命令行内联可规避。
6. **响应式**：整体按 1920×1080 设计；表头与异常订单数字用 `clamp(px, vw, px)` 随窗口缩放，避免窄屏溢出。
7. **公开仓库安全**：密码/凭据不要明文入库，用哈希；`rawdata/`（患者数据）在 `.gitignore` 排除。
8. **模板遗留清理**：`echarts_1/echarts_4` 死函数、静态 option 定义、Bubble.js、44.json、area_echarts.js 等均已移除或 gitignore。
9. **GitHub 分支名**：`git init` 默认 `master`，仓库默认 `main`，推送前需 `git branch -M main`（已内置在 deploy.sh）。
10. **CSS 渐变语法兼容**：`radial-gradient(50% 35%, farthest-corner, ...)` 两个尺寸说明符冲突，现代浏览器判为无效、整条声明被忽略 → 登录遮罩变透明露出数据（body 因有实色兜底所以没崩）。修复：`background-color` 实色兜底 + `-webkit-radial-gradient` 旧语法 + `radial-gradient(farthest-corner at 50% 35%, ...)` 正确新语法，三层保证不透。
11. **第二排整行自适应**：`.nav2>li` 高度从固定 `240px` 改为 `clamp(200px, calc(100vh - 720px), 380px)` 并设 `display:flex; flex-direction:column`；四个图表容器统一 `flex:1` 撑满，删除写死的 `200px` / `calc(100% - 40px)`，否则行高变了图表还固定、留出大片空档。
12. **ECharts 柱状图顶部标签裁切**：柱值恰为 yAxis 最大值时柱顶贴绘图区顶边，`position:'top'` 的标签被裁掉。修复：yAxis `max` 回调加 25% 余量 + `grid.top` 提到 14%；柱宽用百分比（`barWidth:'12%'`）随容器宽度缩放，窄屏不溢出。
13. **登录门禁与后台动画解耦**：遮罩盖住页面时后台地图仍轮播（浪费且暴露动态）。轮播改为登录后才启动——map.js 加载时判断 `sessionStorage.board_authed`，未登录只挂 `window.startBoardAuto()`，登录脚本密码校验通过后调用。
14. **小省份/直辖市点不到**：天津/上海/北京区域小但散点大，点击被散点截获（`componentType=series`），geo 省份点击永远触发不了。修复：click 处理器加 series 分支，用 `CITY_PROV[城市]` + 省全名→短名反查表 `fullToKey`，点城市散点直接打开所在省浮窗。
15. **file:// 双击 fetch 被屏蔽**：直接双击 index.html 打开时，浏览器禁止 `fetch()` 读取本地文件，省份地图与右侧 Top5（在 fetch 成功回调里渲染）一起失效。修复：`build_geo.js` 把 34 省 GeoJSON 打包成 `js/geo-data.js` 内联；openProvince 在线优先 fetch（按需快），失败再懒加载内联数据兜底。
16. **LAST7 缺当天**：`new Date('2026-08-01')`（ISO）按 UTC 解析=本地 08:00，而截止日 `new Date('2026/08/18')`（斜杠）按本地解析=00:00，8 小时时差使 `_cur<=_end` 在当天不成立，循环永远少一天。修复：`_cur` 也改斜杠格式本地解析。
17. **ECharts 4 map 系列不支持 `geoIndex`**：map 系列绑定 geo 用 `geoIndex` 是 v5 才有，v4 会异常。改用 `geo 组件 + geo.regions 逐国 areaColor` 做热力（颜色手算）、`lines` 做飞线、`effectScatter` 做辐射圆点。
18. **世界地图标签漂移**：无 `properties.cp` 时 ECharts 用坐标平均定位标签，新西兰（多岛+跨反经线）会被拉到海上。修复：注册前按"最大外环坐标平均中心"补 `cp`，飞线端点/轮播定位也用它。
19. **不可见散点 showTip 不渲染**：ECharts4 对 `symbolSize:1` 透明散点 dispatchAction showTip 不显示 tooltip。左图改"鼠标移入区域弹自定义 HTML 数据卡片"（`chart.on('mouseover')` + 定位鼠标像素）。
20. **全屏地图保留交互**：把地图容器移入全屏层 + `resize()`，轮播/点击/下钻天然保留（不重建 chart）；关闭移回原位。全屏开关触发 `boardfs` 事件，各页轮播控制器监听暂停/恢复。
21. **Page2 年份口径与 page1 统一**：下单按下单日期年、回输按回输结束时间年（不再用 dict 首次付款时间），两侧口径一致。
