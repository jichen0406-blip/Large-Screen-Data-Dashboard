# 驯鹿生物大数据监控平台 — 项目文档

> 目录：`D:\VS Code\fucaso-board`
> 部署仓库：https://github.com/jichen0406-blip/Large-Screen-Data-Dashboard
> 技术栈：HTML + CSS + jQuery + ECharts 4.0.5（中国地图 geo）+ Node.js（数据生成）

---

## 1. 项目概述

数据驱动的大屏看板（多页面，导航 P1 更新日志 / P2 福可苏业绩总览 / P3 福可苏全流程跟进 / P4 海外商业化 / P5 全球注册进度 / P6-P8 辖区数据管理1-3 / P9 预留 + P0 后台管理（仅管理员））。**总览页**（浏览器标签"福可苏业绩总览"）展示驯鹿生物的订单 / 回输 / 单采 / 放行等业务指标；**福可苏全流程跟进页（P3）** 以 13 列漏斗堆积柱状图展示下单→回输全流程各阶段的订单分布，支持按下单日期时间范围筛选；**Page2（海外/商业化）** 展示海外患者 / 海外商业化（导流、NPP、商业化分类）；**Page3（辖区数据管理1）** 展示 KPI 目标达成 + 国内&海外挑战目标月度表；**Page4（辖区数据管理2）** 展示辖区达成 + 海外患者销量月度表；**Page6（辖区数据管理3）** 展示省份/医院 YTD 月度明细。数据来自 Excel（bs_order + masterdata + 异常订单 + order dict + manual bs order + Target 公司目标/挑战目标），由 `build_data.js` 每日生成静态 `js/data.js`（全局 `BOARD_DATA`，含 `P2`/`P3`/`P3T`/`FLOW` 子数据），前端直接引用，无需后端。

页面访问需要登录门禁（账号系统：管理员 administrator / 密码 `fucaso2026`，普通用户按页面权限分配），见 §6.3。

---

## 2. 目录结构

```
fucaso-board/
├── changelog.html        # P1 更新日志：主要新开发功能时间线
├── index.html            # P2 福可苏业绩总览：登录门禁 + 表头 + 第一排 + 第二排图表
├── flow.html             # P3 福可苏全流程跟进：13 列漏斗堆积柱状图 + 下单日期时间范围控制
├── page2.html            # P4 海外/商业化：年份切换 + 4卡片 + 2世界地图 + 明细表 + Top10浮窗
├── page3.html            # P5 全球注册进度：世界地图 + 甘特图
├── page4.html            # P6 辖区数据管理1：时间控制 + YTD/MTD 目标达成 KPI + 月度表1/2
├── page5.html            # P7 辖区数据管理2：辖区达成 3/4 + 患者渠道销量 5
├── page6.html            # P8 辖区数据管理3：省份/医院 YTD 月度明细
├── page7.html            # P0 后台管理（仅管理员）：用户管理 + 页面权限管理 + 生成配置
├── page8.html            # P9 预留
├── css/style.css         # 全部样式
├── build_data.js         # 数据管线：Excel → js/data.js（含 P2 海外/商业化数据、P3 目标达成数据）
├── build_geo.js          # 打包 34 省 GeoJSON → js/geo-data.js（内联，离线兜底）
├── deploy.sh             # 一键部署：生成数据 + 提交 + 推送到 GitHub
├── package.json          # npm run data → node build_data.js
├── .gitignore            # 排除 rawdata/node_modules/遗留死文件
├── images/               # 背景图、logo（logo-white.png 为白字版）
└── js/
    ├── jquery.js / echarts.min.js / china.js   # 第三方库
    ├── data.js           # 【生成文件】BOARD_DATA，勿手改
    ├── geo-data.js       # 【生成文件】34 省 GeoJSON 内联（离线/file:// 兜底）
    ├── world-data.js     # 【生成文件】世界 GeoJSON 内联（含台湾/香港/澳门独立区域）
    ├── js.js             # 总览页主逻辑：滚动、月度趋势、异常订单、COE、Top30、当月时间轴
    ├── map.js            # 总览页地图：散点、自动轮播、省份浮窗
    ├── page_flow.js      # P3 全流程跟进逻辑：13 列漏斗计算 + 下单日期时间范围控制
    ├── page2.js          # P4 海外/商业化逻辑：年份切换、卡片、两世界地图、明细表、Top10
    ├── page3.js          # P5 全球注册进度逻辑：世界地图 + 甘特图
    ├── page4.js          # P6 辖区1逻辑：KPI 目标达成 + 国内&海外月度表（公司DOM&OB目标）
    ├── page5.js          # P7 辖区2逻辑：辖区达成 3/4 + 患者渠道销量 5
    ├── page6.js          # P8 辖区3逻辑：省份/医院 YTD 月度明细
    ├── pt-common.js      # 辖区1/2/3 共享：时间控制（sessionStorage）+ 表格辅助 + 五行单元格渲染
    ├── fs.js             # 全屏弹窗公共脚本（§9 标准）：图表等比缩放 + 表格/地图 resize
    ├── nav.js            # 统一网页导航：P序号命名、每屏4项分页、📌默认首页、当前页徽章；页面清单 BOARD_PAGES
    ├── users.js          # 账号配置（BOARD_USERS：users 账号表 + pageAccess 页面权限），改后部署生效
    ├── auth.js           # 认证：读账号表校验 + sessionStorage 记录权限，暴露 BoardAuth
    ├── admin.js          # 后台管理页逻辑：用户管理 + 页面权限管理 + 生成配置
    └── geo/              # 省份(34) + 世界(world.json) GeoJSON（源文件）
```

---

## 3. 数据逻辑（build_data.js → js/data.js）

### 3.1 数据源

| 文件 | 位置 | 用途 |
|---|---|---|
| `bs_order*.xlsx` | `D:\VS Code\fucaso-dashboard\rawdata\` | 订单主数据 |
| `masterdata.xlsx` | 同上 | 医院主数据（标准名称/省份/城市/COE 标记） |
| `异常订单.xlsx` | 同上 | 月度异常订单（PBMC分选失败/第一次/二次生产失败） |
| `order dict.xlsx` | 同上 | 订单分类标记（导流方式 / 海外导流国家 / 合同号） |

脚本优先读 `fucaso-board/rawdata`，不存在则回退到 `fucaso-dashboard/rawdata`。

### 3.2 医院匹配规则

- 优先 `医疗机构编码`（第 33 列）查 masterMap；
- 为空则用 `处方来源医疗机构编号`（第 6 列）；
- 均无 → 记录到 `unmatched`，**脚本报错退出**（质量门禁，需补 masterdata）；
- 排除新加坡；未匹配医院记为 `未知医院`。

### 3.3 关键口径（与 fucaso-dashboard/build_poster.js 一致）

- **YTD**：当年 1/1 ~ 今天；**MTD**：当月 1 日 ~ 今天；
- **同比**：去年同截止日（`去年 + 当年月日`），去年为 0 → 返回 null，前端显示 `--`；
- 数据截止日 `DP` = 当天日期。

### 3.4 BOARD_DATA 字段

| 字段 | 内容 |
|---|---|
| `UPDATED` | bs_order 文件修改时间（表头"数据更新时间"） |
| `DP` / `Y` | 数据截止日 / 年份 |
| `YTD{O,R,A,Q}` / `MTD{O,R,A,Q}` | 下单/回输/单采/放行，全年 / 当月 |
| `MONTH{O,R,OLY,RLY}` | 当年+去年各 12 个月下单/回输（月度趋势双折线） |
| `PROV` / `CITY` | 省份 / 城市 → {o,r,a,q} |
| `TOP_O` / `TOP_R` | Top30 下单 / 回输医院（含同比） |
| `LAST7` | **当月每天**（1 号~今天）下单/回输医院明细（时间轴） |
| `HOSP_PROV{O,R}` | 各省 Top5 下单/回输医院（省份浮窗右侧榜单） |
| `COE{O,R}` | SCOE/COE/RCOE/Others 四类销量（COE 占比饼图） |
| `ABN` | 当年异常订单：total/pbmc/first/second |
| `CITY_PROV` | 城市 → 省份（大地图轮播按省份分组） |
| `P2` | Page2 数据：`YEARS` 年份 / `CARD` 卡片 / `LEFT` 海外患者 / `RIGHT` 海外商业化 / `RIGHTD` 国家×导流分类 / `TOP10` 医院 |
| `P3` | Page3 数据：`TARGET` 月度目标（键 `YYYY-MM`→`{o:下单,r:回输}`）/ `MONTH_O` 月度下单 / `MONTH_R` 月度回输 |
| `FLOW` | 全流程跟进数据：`orders` 订单明细数组（每单 `no/od/pay/apmt/receive/qa/re/cancel/resume/note/modZq`，前端按时间段计算 13 列漏斗） |

---

## 4. 页面结构与功能

### 4.1 表头
- **logo**（`images/logo-white.png`，驯鹿生物+iaso 白字版）+ 标题"驯鹿生物大数据监控平台"居中；浏览器标签标题为"福可苏业绩总览"；
- 左侧：**数据更新时间**（`B.UPDATED`）+ **"☰ 网页导航"按钮**（`js/nav.js` 统一渲染：P1-P5 命名、每屏最多 4 项 + 页码条、📌 设默认首页）；
- 右侧：**时钟**（内联脚本每秒刷新）+ **上海天气**（Open-Meteo 实时接口）。

### 4.2 第一排（22% / 56% / 22%）
- **左**：Top30 下单 / 回输医院表格（表头 sticky，内容**自动无缝滚动**，悬停暂停）；
- **中**：中国地图
  - YTD / MTD 各 4 个卡片（左右两侧，`space-evenly` 均布）；
  - 雷达扫描动画（`.radar-sweep` conic-gradient 旋转）；
  - 城市散点 + **自动轮播**：按省份北→南、省内纬度排序，逐城市高亮展示卡片（5s 一个）；**登录后才启动**（未登录不轮播，登录成功后由登录脚本触发）；
  - **点击省份 或 城市散点** → 浮窗显示该省/市地图 + 城市散点 + 右侧 Top5 下单/回输医院榜单；地图数据在线走 fetch 按需加载，离线（file://）自动懒加载内联 `js/geo-data.js`；
- **右**：**当月**每天下单/回输时间轴（自动滚动，表头 sticky）。

### 4.3 第二排（4 个 25%）
- **下单月度趋势** / **回输月度趋势**：双折线——当年：下单蓝 / 回输绿，去年统一亮灰；折线上方逐月同比（绿↑/红↓/灰--，0 位小数，无数据月不显示）；
- **YTD异常订单**：左侧红色大数字（当年合计，随屏缩放 `clamp`）+ 右侧三类失败柱状图（黄/橙/红）；整行高度随视口缩放（`.nav2>li` `clamp(200px, calc(100vh - 720px), 380px)`），柱宽百分比、yAxis 头部余量，标签不裁切；
- **COE医院销量占比**：两个环形饼图（下单 / 回输），中心标题，底部图例，扇区内显示占比，四类配色 SCOE 绿 / COE 蓝 / RCOE 青 / Others 黄。

### 4.4 Page2（海外患者 / 海外商业化）
- 表头同总览页（浏览器标签"福可苏业绩总览 · Page 2"）；登录 sessionStorage 共享，总览登录过则免密；
- **年份切换**（第一行第 5 个容器，横向平铺整页）：全部 + 数据实际年份（2027 等自动出现）；**口径与 page1 统一**——下单按下单日期年、回输按回输结束时间年；
- **4 张卡片**（占 4/5 页宽，间距自适应）：NPP/商业化 下单/回输、国内导流(医生导流+OB导流) 下单/回输（导流列为空的国内直接患者不计入）；
- **左图 海外患者（含导流以及海外商业化）**：热力色阶 + 各国飞向中国的飞线 + 中国中心驯鹿生物中文 logo（白字 + 橙色鹿，512px 透明底轻量版）+ 上方深蓝高光点（带辐射光圈）；**不轮播，鼠标移入区域展示下单/回输卡片**；
- **右图 海外商业化**：仅 NPP/商业化，**圆点向外辐射**（大小随销量）；自动轮播（5s）+ 点击区域弹出 Top10 医院浮窗（左下单/右回输）；
- **地图下方明细表**：大洲合并同类（rowspan）、先按大洲合计下单降序、洲内按国家下单降序、固定 10 行补空白、表头锁死；左表自动滚动（80ms/1px，悬停暂停）；右表多一列"导流分类"（医生导流/OB导流/NPP/商业化）；
- **地图放大全屏**：每张地图右上角 ⛶，容器移入全屏层并 `resize()`，交互保留；点 × / 空白 / Esc 恢复；
- **网页导航菜单**：`js/nav.js` 统一渲染 P1-P5（每屏最多 4 项 + 页码条），📌 设默认首页（localStorage），从站点外部首次进入自动跳转。

### 4.5 Page3（辖区数据管理1）
- 表头同总览页（浏览器标签"福可苏业绩总览 · 辖区数据管理1"）；登录 sessionStorage 共享；
- **时间控制**：年/月下拉选择器（默认当前月）；下单按下单日期、回输按回输日期**分别截止**到所选月（同一订单下单/回输可在不同月，两个字段独立统计）；
- **左侧 YTD（截至所选月）** 下单 / 回输各 4 卡：目标 / 达成 / 达成率 / 同比；
- **右侧 MTD（所选月）** 下单 / 回输各 4 卡：目标 / 达成 / 达成率 / 同比；
- **同比口径**：今年 1月-所选月完整月 vs 去年 1月-所选月完整月（当月未结束也用完整月），与 page1 统一；
- **目标数据**：`Target.xlsx` 公司目标 sheet（月度下单/回输目标；YTD=1月-所选月累计，MTD=当月）；
- 卡片名称 icon 为**白底灰边圆徽章**（彩色 SVG，无荧光），与深蓝卡片背景形成对比；
- 计算逻辑在 `js/page3.js`；数据生成在 `build_data.js`（读 Target.xlsx + 月度实际 → `BOARD_DATA.P3`）。

### 4.6 挑战目标 / 辖区 月度表（12月+YTD 全列，受时间控制）
分布在 **P3（辖区数据管理1）** 与 **P4（辖区数据管理2）**，左右分栏 + 全宽；**时间控制 P3/P4 共享**（localStorage `p3t_time`，任一侧改选即同步）；YTD=1月-所选月累计、选中月高亮，每张表标题栏有 ⛶ 全屏（§9 标准）。

- **P3**：1. 国内&海外 下单月度、2. 国内&海外 回输月度（**公司目标**：DOM 国内 / OB 海外）；
- **P4**：3. 辖区下单达成、4. 辖区回输达成（8 位 AM + 香港/新加坡/沙特，无 国内/Total 汇总行）、5. 患者渠道销量（首列"类型"区分下单/回输，仅达成白字，Total 蓝色行）。

**数据来源（`build_data.js` → `BOARD_DATA.P3T`）**：
- **目标**：表格 1/2 用 `Target.xlsx`「公司DOM&OB目标」sheet（分类 DOM=国内 / OB=海外）；表格 3/4 用「挑战目标」sheet（YM/Region/AM；Region=DOM→国内、HK→香港、SG→新加坡、KSA→沙特）（**仅 2026 有目标**，其余年份目标显示 `--`）；
- **达成归属**（bs_order 现算，下单按合同创建日期、回输按实际回输结束时间）：
  - **地区**：masterdata 医院 Region（回输与下单统一用**下单医院**；回输医院字段基本全空，不采用）；
  - **下单 AM**：orderdict「AM」列 → 空则 masterdata 医院AM；
  - **回输 AM**：orderdict「回输AM」→ 空则「AM」→ 空则 masterdata 医院AM；
  - AM 均经 masterdata **Sheet3 清洗**（历史离职AM→现职）；AM 为 HK_AM1/SG_AM/KSA_AM 时按对应海外地区归属；
- **LY / 同比**：同逻辑算去年（bs_order 含 2023-2026）。
- 注：`表格数据.xlsx`（参考模板）为上周五快照，当前按最新 bs_order 计算，数字会更新。

### 4.7 左侧悬浮导航栏 + P5 彩蛋
- **左侧悬浮导航栏**（所有页面）：白色 icon 常驻（P1 仪表盘 / P2 地球 / P3 表格 / P4 医院 / P5 设置）+ hover 展开「编号+名称」+ 当前页青色高亮；桌面端固定（`position:fixed; top:72px`）不随滚动，高度对齐 mainbox（`sizeSideNav` + ResizeObserver 同步）；≥769px 隐藏头部 ☰ 按钮；由 `js/nav.js` 的 `renderSideNav` 统一渲染（icon 配置在 `NAV_ICONS`）。
- **P5 彩蛋**：预留页改为"猜猜下一页"彩蛋（🎁 跳动礼物盒 + 青色渐变标题 + 4 个调皮选项 + 底部提示），纯装饰无数据功能。

### 4.8 后台管理（P0，仅管理员）
- 导航入口 `P0 后台管理`（导航最后，仅 administrator 可见）；普通用户不可见，直接访问自动重定向；
- **用户管理**：账号列表（角色 管理员/用户、状态 启用/禁用），新增账号、禁用/启用、重置密码、删除；administrator 为超级管理员（不可删/禁/改角色）；
- **页面权限管理**：每个页面（P1-P5 + 未来新增，读 `window.BOARD_PAGES`）下拉多选可访问用户；**新建页面在 nav.js 的 `BOARD_PAGES` 追加一项，即自动出现在权限管理**；
- **生成配置（本地同步，方案B）**：管理员修改后点「生成配置」→ 调本地 `sync_server.js`（`http://127.0.0.1:62001`）**自动写本地 `js/users.js`**（本地立即生效）；生产目录有 git 时**自动提交并推送 GitHub**（线上生效）；服务未运行时回退手动复制替换；
- **本地同步服务**：`node sync_server.js`（生产/测试目录各一份；生产目录自动 commit+push，测试目录仅写文件）；需与本地预览服务一起常驻；已在 .gitignore 排除（本地管理工具不部署）；
- **数据模型**：`BOARD_USERS = { users: {账号:{pwdHash, role, enabled}}, pageAccess: {页面:[用户名]} }`（admin 角色无需在 pageAccess 配置，自动全部）。

### 4.9 福可苏全流程跟进（P3）
- 表头同总览页（浏览器标签"福可苏业绩总览 · 福可苏全流程跟进"）；登录 sessionStorage 共享；
- **时间范围控制**：起/止「下单日期」年月下拉（**仅本页**，不与其他页共享 sessionStorage），默认起=最早有数据月份、止=最新月份（即全量数据）；起止联动（起始月 > 结束月时自动对齐）；
- **13 列漏斗堆积柱状图**：按「下单 → 择期生产（没单采/过期/已单采）→ 单采预约前取消 → 预约单采中 → 单采前取消 → 等待单采 → 单采中 → 单采后取消 → 排期/生产中 → 生产中取消（生产失败/患者死亡）→ 质量放行 → 生产完成取消回输 → 回输」顺序逐级刨除，每列为一个堆积柱（有颜色段=该阶段人数，透明段=已刨除/剩余），最多 4 层堆叠；
- **tooltip 仅显示有颜色数据段**；无颜色段 100% 透明；柱顶置项顺序按 ECharts `series[0]` 在底部反序处理；
- **异常提示**：择期生产订单若存在「质量放行时间」会在图表上方红色提示列出订单号；
- 数据来自 `BOARD_DATA.FLOW.orders`（`build_data.js` 读 order dict.xlsx 的 取消标记/恢复生产时间/备注/修改择期 字段 + bs_order 的 下单/单采预约/单采血入库/放行/回输 时间）；计算逻辑在 `js/page_flow.js`。

---

## 5. 代码要点

### 5.1 js.js
| 函数 | 作用 |
|---|---|
| `countUp(el,target)` | 数字滚动动画（easeOutCubic，1.2s） |
| `setupTblScroll / setupSevScroll` | 无缝滚动：复制内容一份，`scrollTop` 到达一份高度即回绕（setInterval 80ms/1px，悬停暂停） |
| `monthlyTrend()` | 渲染 echart6 / echart4 双折线（下单当年蓝、回输当年绿，颜色参数可配） |
| `echarts_31()` | 渲染 fb03/fb04 COE 饼图 + myd1 异常订单柱状图（柱宽百分比、yAxis 余量防标签裁切） |
| `board_ext()` | MTD 数字、异常订单总数、更新时间、天气、Top30 表格、当月时间轴 |

### 5.2 map.js
- `playOrder` 构建：`B.CITY_PROV` 按省分组 → 省份按平均纬度降序 → 省内城市按纬度降序 → `showNextCity` 顺序轮播；轮播受登录门控（`window.startBoardAuto`，登录脚本成功校验后调用）；
- 点击交互：点省份边界 → `openProvince(省份)`；点城市散点 → 用 `fullToKey`（省全名→短名）反查并打开所在省（解决直辖市/小省圆点盖住区域点不到的问题）；
- `openProvince` 加载地图：在线优先 `fetch('js/geo/{adcode}.json' or '{adcode}_full.json')`（按需快）；失败则懒加载 `js/geo-data.js` 内联数据（file:// 离线场景）；
- 直辖市（北京/天津/上海/重庆/港/澳/台）用 `{adcode}.json`（自身边界），普通省用 `_full.json`（城市边界）；
- 浮窗打开时 `panelOpen=true` 停止轮播，关闭恢复；geo 组件 `silent:true` 避免红字标签。

### 5.3 page2.js
| 函数 | 作用 |
|---|---|
| `countUp2` | 卡片数字滚动（与 page1 countUp 同款，Page2 独立实现） |
| `mapOption(md, withLines, radiate)` | 共用 option 构造：左图(热力+飞线+中国标记) / 右图(辐射圆点) |
| `startAuto / chartPause` | 右图轮播（按纬度北→南，5s 一个）与暂停 |
| `fillTbl / fillTblRight` | 明细表渲染：大洲合并、按下单排序、固定 10 行补空白 |
| `setupTblAutoScroll` | 左表自动滚动（复制 tbody、80ms/1px、悬停暂停） |
| `openTop10` | 右图点击 → Top10 医院浮窗 |

---

## 6. 接口与配置

### 6.1 天气接口（实时，需外网）
```
https://api.open-meteo.com/v1/forecast?latitude=31.23&longitude=121.47&current=temperature_2m,weather_code&timezone=Asia%2FShanghai
```
- 前端 fetch，无 key、支持跨域；失败显示"上海 --"；
- 天气码映射为符号：☀晴 / ⛅多云 / ☁阴 / ☔雨(☔☔中雨 / ☔☔☔大雨) / ❄雪 / ⛈雷阵雨。

### 6.2 省份地图（本地 + 内联兜底）
- `js/geo/` 预下载 34 个省份 GeoJSON（来源：阿里 DataV GeoAtlas）；`build_geo.js` 可打包为 `js/geo-data.js`（内联 `window.GEO_DATA`）；
- 加载策略：在线（GitHub Pages / 本地服务）优先 fetch 单文件按需加载（快）；`file://` 双击打开时浏览器屏蔽 fetch → 自动懒加载 `js/geo-data.js` 内联数据，无需外网/服务；
- 台湾无 `_full` 数据，特判用 `710000.json`。

### 6.5 世界地图（本地内联）
- `js/geo/world.json`（echarts 4.9.0 世界地图源）→ 生成 `js/world-data.js` 内联（`window.WORLD_GEO`，末尾追加台湾/香港/澳门独立区域）；
- 页面加载时把英文区域名改中文、补 `cp` 标签锚点（**最大外环坐标平均中心**，避免新西兰等多岛/跨反经线国家标签漂到海上）后 `registerMap('world')`；
- 内联脚本标签加载，file:// 双击离线可用。

### 6.3 登录门禁（账号权限系统）
- **登录页**（login_29 模板改编）：驯鹿 logo + 两行标题「驯鹿生物大数据监控平台 / 登录页」；动态背景——背景慢速缩放平移（Ken Burns 22s）+ 左侧球体悬浮 + 底部波浪动画；
- **账号系统**：`js/users.js` 定义 `BOARD_USERS`（users 账号表 + pageAccess 页面权限）；登录校验账号存在、已启用、密码 SHA-256 哈希匹配；
- **记住密码**：登录页「记住密码」复选框勾选后，账号+密码明文存 localStorage（`board_remember`，仅本地浏览器、非服务器），下次打开自动预填；未勾选登录则清除该项；
- **管理员** `administrator` / 密码 `fucaso2026`（role=admin，自动拥有全部页面权限 + 后台管理入口 P0）；
- 登录后记录 sessionStorage `board_user`（用户、角色、可访问页面 pages）；普通用户可访问页面 = pageAccess 中含该用户的页面，导航只显示有权限的页，直接访问无权限页自动重定向到首个有权限页；
- 同浏览器会话刷新免登录；
- 登录遮罩 `.login-mask`：login_29 背景图（images/login29/）+ 渐变兜底，登录时完全遮住后面数据；
- 地图轮播在登录前不启动：map.js 加载时检查登录态，未登录仅暴露 `window.startBoardAuto`，登录成功后由登录脚本调用启动；
- 注意：**纯前端门禁，非真正安全**（源码可见校验逻辑）。

### 6.4 部署（GitHub）
- 远程：`git@github.com:jichen0406-blip/Large-Screen-Data-Dashboard.git`（SSH 认证，分支 main）；
- 提交身份：`jichen0406-blip` + noreply 邮箱（在 `.git/config`）；
- 一键命令：`cd "D:/VS Code/fucaso-board" && bash deploy.sh`；
- **标准（账号权限独立管理）**：账号权限文件 `js/users.js` 只由**本地生产环境**的 `sync_server.js` 管理推送，其他任何流程不得覆盖：
  - **测试 → 生产同步**：从测试环境复制代码到生产时，**排除 `js/users.js`**（测试的账号配置不带到生产）与 `sync_server.js`（各自目录的本地工具）；生产账号文件保持生产本地版本；
  - **生产 → GitHub 部署**：`deploy.sh` 提交时**排除 `js/users.js`**，避免部署覆盖账号配置；
  - **GitHub 网页端**：不应直接编辑 `js/users.js`；需改账号时回到本地生产环境的管理页操作（管理页在非本地环境会提示"请回到本地生产环境"）；
  - **推送前自动备份**：`sync_server.js` 每次写入 users.js 前，先把当前版本备份到 `js/users.js.bak`（**覆盖式只保留上一份**），防止新配置损坏后可回退；`.gitignore` 已排除 `.bak` 不提交。

---

## 7. 日常操作

### 7.1 更新数据 + 部署
1. 把最新 `bs_order*.xlsx`、`masterdata.xlsx`、`异常订单.xlsx` 放入 `D:\VS Code\fucaso-dashboard\rawdata\`，**先关闭 Excel**；
2. 运行：`bash deploy.sh`（自动：node build_data.js → node build_geo.js → git commit → push）；
3. 浏览器 **Ctrl+F5 强刷**查看。

### 7.2 修改密码（账号系统）
1. 管理员登录后台管理 P0 → 用户管理 → 目标账号「重置密码」，输入新密码（前端自动 SHA-256 哈希）；
2. 点「生成配置」→ 本地 sync_server.js 写 js/users.js（本地立即生效；生产目录自动提交并推送 GitHub；服务未运行则手动复制替换，见 §4.8）；
3. 被改账号下次登录生效。

### 7.3 本地预览
```
cd "D:/VS Code/fucaso-board" && npx serve -p 53202
```

---

## 8. 实施经验 / 踩坑记录

1. **ECharts 4.0.5 的 geo 强调态语法**：省份地图红色城市名问题——v5 的顶层 `emphasis:{label:...}` 在 v4 不生效，必须用 v4 嵌套写法 `label:{normal:{...}, emphasis:{show:false}}`；彻底去掉区域交互则设 `geo.silent:true`。
2. **无缝滚动的"不滚动"坑**：rAF + 亚像素（`scrollTop += dt/240`）在某些浏览器不生效；改用 `setInterval(80ms) + 整数 1px` 步长最可靠；内容复制一份后按"一份高度"回绕实现无缝。
3. **CSS 优先级**：`.wancheng span` 会把数字字号强制成 14px，覆盖 h3 继承——子元素被更通用规则命中时，需给目标元素单独加更高优先级规则（`.wancheng h3.abn-num #abnNum`）。
4. **高度溢出**：容器 `height:100%` 会把标题（tit01）高度也算进去导致图表被底部裁切，需 `height:calc(100% - 40px)` 修正。
5. **中文路径编码**：Windows + Node 直接 `require` 含中文路径的 xlsx 可能 ENOENT，用 `path.join` + 脚本文件（UTF-8）而非命令行内联可规避。
6. **响应式**：整体按 1920×1080 设计；表头与异常订单数字用 `clamp(px, vw, px)` 随窗口缩放，避免窄屏溢出。
7. **公开仓库安全**：密码/凭据不要明文入库，用哈希；`rawdata/`（患者数据）在 `.gitignore` 排除。
8. **模板遗留清理**：`echarts_1/echarts_4` 死函数、静态 option 定义、Bubble.js、44.json、area_echarts.js 等均已移除或 gitignore。
9. **GitHub 分支名**：`git init` 默认 `master`，仓库默认 `main`，推送前需 `git branch -M main`（已内置在 deploy.sh）。
10. **CSS 渐变语法兼容**：`radial-gradient(50% 35%, farthest-corner, ...)` 两个尺寸说明符冲突，现代浏览器判为无效、整条声明被忽略 → 登录遮罩变透明露出数据（body 因有实色兜底所以没崩）。修复：`background-color` 实色兜底 + `-webkit-radial-gradient` 旧语法 + `radial-gradient(farthest-corner at 50% 35%, ...)` 正确新语法，三层保证不透。
11. **第二排整行自适应**：`.nav2>li` 高度从固定 `240px` 改为 `clamp(200px, calc(100vh - 720px), 380px)` 并设 `display:flex; flex-direction:column`；四个图表容器统一 `flex:1` 撑满，删除写死的 `200px` / `calc(100% - 40px)`，否则行高变了图表还固定、留出大片空档。
12. **ECharts 柱状图顶部标签裁切**：柱值恰为 yAxis 最大值时柱顶贴绘图区顶边，`position:'top'` 的标签被裁掉。修复：yAxis `max` 回调加 25% 余量 + `grid.top` 提到 14%；柱宽用百分比（`barWidth:'12%'`）随容器宽度缩放，窄屏不溢出。
13. **登录门禁与后台动画解耦**：遮罩盖住页面时后台地图仍轮播（浪费且暴露动态）。轮播改为登录后才启动——map.js 加载时判断 `sessionStorage.board_authed`，未登录只挂 `window.startBoardAuto()`，登录脚本密码校验通过后调用。
14. **小省份/直辖市点不到**：天津/上海/北京区域小但散点大，点击被散点截获（`componentType=series`），geo 省份点击永远触发不了。修复：click 处理器加 series 分支，用 `CITY_PROV[城市]` + 省全名→短名反查表 `fullToKey`，点城市散点直接打开所在省浮窗。
15. **file:// 双击 fetch 被屏蔽**：直接双击 index.html 打开时，浏览器禁止 `fetch()` 读取本地文件，省份地图与右侧 Top5（在 fetch 成功回调里渲染）一起失效。修复：`build_geo.js` 把 34 省 GeoJSON 打包成 `js/geo-data.js` 内联；openProvince 在线优先 fetch（按需快），失败再懒加载内联数据兜底。
16. **LAST7 缺当天**：`new Date('2026-08-01')`（ISO）按 UTC 解析=本地 08:00，而截止日 `new Date('2026/08/18')`（斜杠）按本地解析=00:00，8 小时时差使 `_cur<=_end` 在当天不成立，循环永远少一天。修复：`_cur` 也改斜杠格式本地解析。
17. **ECharts 4 map 系列不支持 `geoIndex`**：map 系列绑定 geo 用 `geoIndex` 是 v5 才有，v4 会异常。改用 `geo 组件 + geo.regions 逐国 areaColor` 做热力（颜色手算）、`lines` 做飞线、`effectScatter` 做辐射圆点。
18. **世界地图标签漂移**：无 `properties.cp` 时 ECharts 用坐标平均定位标签，新西兰（多岛+跨反经线）会被拉到海上。修复：注册前按"最大外环坐标平均中心"补 `cp`，飞线端点/轮播定位也用它。
19. **不可见散点 showTip 不渲染**：ECharts4 对 `symbolSize:1` 透明散点 dispatchAction showTip 不显示 tooltip。左图改"鼠标移入区域弹自定义 HTML 数据卡片"（`chart.on('mouseover')` + 定位鼠标像素）。
20. **全屏地图保留交互**：把地图容器移入全屏层 + `resize()`，轮播/点击/下钻天然保留（不重建 chart）；关闭移回原位。全屏开关触发 `boardfs` 事件，各页轮播控制器监听暂停/恢复。
21. **Page2 年份口径与 page1 统一**：下单按下单日期年、回输按回输结束时间年（不再用 dict 首次付款时间），两侧口径一致。
22. **批量脚本误删事故 / 删除前先备份**：用 Node 脚本批量移除各页内联 fs 脚本时，`indexOf('地图放大全屏')` 定位到了 fs-mask 的 HTML 注释而非脚本本身，把 mainbox/登录门禁/fs-mask/菜单等主体整段删掉，5 页全损坏，只能从生产环境复制 + 重建恢复。教训：批量改文件前先备份或先小范围验证定位逻辑；已在全局 `~/.claude/CLAUDE.md` 加入"删除/覆盖前先备份"规则。
23. **公共脚本抽取（fs.js）**：全屏脚本原来各页一份且已漂移（page3 有图表分支、page4 是简化版）。抽成 `js/fs.js` 统一引用；isChart 表 id、`fsExtra`(data-fs2)、page2 表格行展开钩子 `p2SetTblRows` 均用存在性守卫，各页自动兼容（无则走普通 resize）。
24. **P3/P4 时间控制共享用 sessionStorage 而非 localStorage**：localStorage 跨会话持久，下次打开网页还停留在上次选的年月；改 sessionStorage 后，同一标签页内 P3↔P4 导航共享选择、新开网页默认当前年月。
25. **表格五行渲染去重**：page3/page4 重复的辅助函数（pad/mVal/pct2/yoyStr）与五行单元格逻辑抽到 `js/p3t-common.js`，两个页面各减约 55 行重复；页内选择器补了无障碍 title。
26. **build_data.js 性能**：同一 xlsx 多次 `XLSX.readFile`（Target 3 次、masterdata 3 次、orderdict 2 次）→ 缓存 workbook 复用，减少约 2/3 解析耗时；P3T 下单/回输统计循环去重为 `addP3`，并删了从未使用的 `mdByCode` 死代码。
27. **登录门禁升级（单密码 → 账号权限系统）**：原 index.html 内联 HASH 单密码校验改为 `js/users.js`（`BOARD_USERS`）+ `js/auth.js`（`BoardAuth`）+ 后台管理 P0；所有页面统一引用 users.js/auth.js，删除各页内联登录脚本；修改密码流程改为管理页重置（§7.2）。登录页新增「记住密码」（明文存 localStorage，仅本地浏览器，属用户确认的便利设计，见 §6.3）。

---

## 9. 弹窗（全屏）功能开发标准

> 适用：所有图表 / 地图 / 表格的"放大全屏"功能。后续新增可展示块一律按此标准实现，保证交互一致、可维护。

### 9.1 需求规范（验收标准）
1. **每个可展示块**（图表 / 地图 / 表格 / 列表）右上角提供 ⛶ 全屏按钮；
2. **按钮位置**：与标题栏（`.tit` / `.tit01`）同一行、右侧、垂直居中；无标题栏的块（如中国地图）放容器右上角；
3. 全屏**保留原交互**：点击、悬停 tooltip、地图下钻、省份浮窗等不丢失；
4. 全屏期间**所有自动轮播 / 自动滚动停止**，关闭后恢复；
5. **关闭后主页面完全恢复**（无需刷新）；
6. 手机端可用（媒体查询适配）。

### 9.2 实现规范
- **容器**：`#fsMask`（fixed 全屏遮罩，flex column）+ `#fsHead`（标题 + ×）+ `#fsBody`（内容区）。
- **按钮**：`<button class="map-fs" data-fs="目标元素id" data-title="弹窗标题">⛶</button>`，放标题栏内（`.tit`/`.tit01` 需 `position:relative` + 预留 `padding-right`）。
- **按类型区分全屏方式**：
  | 类型 | 全屏实现 |
  |---|---|
  | 地图 | 容器移入 `#fsBody` + `echarts.resize()` 填满 |
  | 图表（ECharts） | 记录原宽高 → 按屏幕**等比缩放**（contain 居中）→ `resize()` 到实际放大尺寸（高清）→ `scaleChartFonts()` 按比例放大全部字号 |
  | 表格/列表 | 容器移入 + `height:100% !important` + 全屏字号统一（数据 14px/行高 26px、表头 14px/30px）+ 去除无缝滚动复制的重复内容 + 停止自动滚动 |
- **轮播/滚动控制**：全屏开/关在 fs 脚本触发 `$(document).trigger('boardfs', {active:true/false})`；各轮播控制器监听 `boardfs` 暂停/恢复（地图轮播、page2 右图、Top30/时间轴/明细表滚动均已接入）。
- **关闭恢复**：① 用**局部变量**保存元素（禁止在 `setTimeout` 回调里引用会被置空的循环变量）；② 清除所有内联样式（width/height/transform/position 等）；③ 图表字号恢复用全屏前保存的原 `option` 快照 `setOption` 还原。

### 9.3 踩坑清单（务必避免）
1. **按钮放进 ECharts 容器会被画布遮挡** → 放标题栏或容器外；
2. **transform scale 视觉放大会模糊** → 图表必须 `resize()` 到实际放大尺寸，字号用 `scaleChartFonts` 放大；
3. **ECharts `fontSize` 可能是字符串**（如 `'11'`）→ 缩放逻辑要同时处理 `number` 和 `string`；
4. **表格无缝滚动复制了两份内容** → 全屏后显示两遍，需去重；去重循环里 `rows.length` 会随 `removeChild` 动态变小，**必须先用数组快照固定边界**，否则过度删除只剩一行；
5. **字号放大要作用到子元素**：子元素（`li`/`span`/`.ts-o`）自身有固定 `font-size` 会覆盖父级继承，需针对最内层元素设字号；
6. **CSS 特异性**：id 选择器（如 `#tbl1body{height:200px}`）会压过类规则 → 全屏高度用 `.fs-body .xxx{height:100% !important}`；
7. **hover 的 `mouseleave` 会重新触发自动滚动** → 全屏期间用 `_fsPause` 标志拦截；
8. **闭包陷阱**：`setTimeout(function(){ resizeChart(fsEl); }, 50); fsEl = null;` 回调执行时 `fsEl` 已是 null → 用 `var _el = fsEl` 局部保存；
9. **sticky 表头 + rowspan 表格**在 auto 布局下起始行可能被拉高 → `table-layout: fixed` 稳定布局。

---

## 10. 开发流程标准（每次开发后必须执行）

> 任何功能开发 / 修改完成后，在交付部署前必须做以下检查，缺一不可。

### 10.1 手机自适应检查（≤768px）
用 DevTools 移动模拟（375px 宽）逐项验证：

1. **布局**
   - 表头：更新时间 / 导航按钮 / 时间天气不重叠，自动换行居中；
   - 第一 / 第二排纵向堆叠正常，无横向溢出；
   - 图表容器高度不塌陷（flex 布局 + 固定高度兜底）；
   - 表格 / 列表不横向溢出。
2. **交互**
   - 全屏按钮可点（触控目标 ≥26px）；
   - 全屏弹窗：图表等比 contain、表格行压缩、时间轴日期列窄化；
   - 关闭全屏后主页面完全恢复（无需刷新）；
   - 全屏期间所有自动轮播 / 滚动停止，关闭恢复。
3. **样式**
   - `clamp()` 字号随屏缩放、数字不溢出卡片；
   - 浮窗（网页导航 / Top10 / 省份）宽度 ≤ 视口；
   - 登录框不溢出。

### 10.2 新增可展示块 checklist
新增图表 / 地图 / 表格时必须：
- [ ] 按 §9 弹窗标准实现 ⛶ 全屏（按钮在标题栏、类型区分、boardfs 轮播控制、关闭恢复）；
- [ ] 在 `@media (max-width: 768px)` 中补齐该块手机端适配；
- [ ] 全屏字号统一（表格 14px / 行高 26px，时间轴医院 14px）。

### 10.3 常用适配手段（速查）
- 媒体查询 `@media (max-width: 768px)` 集中管理手机端；
- 图表容器固定高度（`.nav2 .ftechart{height:240px}`）防 `flex:1` 塌陷；
- 表格行高 / 字号压缩、时间轴日期列窄化（80px）；
- `flex-wrap` 让卡片 / 容器换行（如 Page2 卡片 2 列）；
- `clamp(px, vw, px)` 字号随屏缩放；
- 全屏高度用 `.fs-body .xxx{height:100% !important}` 压过 id 选择器。

---

## 11. 新建页标准操作

> 新增页面（Page3、Page4…）时按此标准执行。模板任意一个已建页（如 `page4.html` 辖区数据管理1）即完整示范。
>
> **核心原则：页面身份 = 稳定识别码 `key`，不依赖文件名、不依赖 P 序号。** 增删页只改 mapping 表（`nav.js BOARD_PAGES` 的 num/name/id），各页 `key` 与权限 `pageAccess` 永不顺移，因此授权不会乱。

新增页面分两种场景，先判断属于哪种再套对应步骤：

- **顺序新增（末尾追加）**：新页面加在现有最后一位之后，`P` 序号递增。只改 `nav.js` 追加一项 + `users.js` 加新键 + 新建页面文件，不动现有页面——按 §11.1-11.4。
- **插入新增（中间顺移）**：新页面插在某个现有页面之前（如插在 P3 位置，原 P3 及之后 `P` 序号整体后移一位）。**不再改任何文件名、不再移动任何文件**，只改 `nav.js BOARD_PAGES` 的 num/name + 新建页面文件——按 §11.5。

### 11.1 复制模板（框架自动配置）
以任一已建页面为模板复制，已内置：
- **表头**：logo + 主标题 + **数据更新时间**（`B.UPDATED`，span id 用 `p{N}Upd`）+ **☰ 网页导航按钮**；
- **时钟 + 上海天气**（内联脚本）；
- **登录门禁**（sessionStorage 共享，全站一次登录）；
- **全屏框架**（fs-mask + fs 脚本，未来加图表直接用）；
- **网页导航**（`js/nav.js` 统一渲染：P 序号命名、每屏最多 4 项 + 页码条、📌 默认首页、当前页徽章、默认首页跳转）；
- **页面识别码**（head 内 `<script>window.PAGE_KEY='{key}';</script>`，放在 `js/users.js` 之前；**key 唯一、语义化、永不变**，随文件内容走）；
- **左侧悬浮导航栏**（每页 `</body>` 前加 `<div class="side-nav" id="sideNav"></div>`，nav.js 自动渲染）。

### 11.2 页面命名
- 文件：`pageN.html`（N 为当前序号，可后续随插入顺移——**文件名不是身份，可改**）；
- `<title>`：`福可苏业绩总览 · {页面名}`；
- 主体 `.tit`：页面名（如"辖区数据管理1"）；
- **导航序号** `P{N}` 与**识别码** `key`：在 `js/nav.js` 的 `BOARD_PAGES` 数组中对应。
- **样式/JS 前缀纪律**：本页专属 CSS 类与元素 id 用 `p{N}-*`（如 `p4-kpi`、`p5t61`）；多页共享用中性前缀 `pt-`（如 `pt-time`、`pt-tbl-wrap`）；避免沿用其它页的旧前缀（曾出现 page4/5 仍用 `.p3-*` 的遗留问题）。

### 11.3 接入导航（nav.js 统一管理，唯一改动点）
新页面只需在 `js/nav.js` 的 `BOARD_PAGES` 数组追加一项（含 `key`）：
```js
{ id: 'pageN.html', key: 'region3', num: 'P6', name: '页面名' },
```
- 所有页面已引用 `js/nav.js`，菜单自动渲染（命名 `P序号（页面名）`、每屏最多 4 项 + 页码条、📌 设为默认首页、当前页徽章）；
- 页面文件本身无需手写菜单项 / 默认首页 / 跳转脚本；
- 页面数超过 4 时菜单自动出现页码条（如 P1-P4 / P5-P8 分页）；
- **新建页面后**：到后台管理 P0 的「页面权限管理」给该页勾选可访问用户（权限管理自动列出新页，见 §4.8）；同时确认 users.js 的 `pageAccess` 含新页的 `key`（键值用 key，不是文件名）。

### 11.4 新增内容
图表 / 地图 / 表格按 §9 弹窗标准 + §10 手机自适应标准实现。

### 11.5 插入新增（页面顺移）
场景：新页面要插在第 `K` 位（如 P3）。**不重命名、不移动任何既有文件**，只新建新页 + 改 mapping 表。以「在 P3 插入，原 P3-P6 序号后移为 P4-P7」为例：

- **第 0 步 · 先备份**：涉及改动的 `nav.js`、`users.js` 及新页模板 `cp xxx xxx.bak`（全局规则：删除/覆盖/批量改前先备份）。
- **第 1 步 · 建新页文件**：复制模板得到新 `page3.html`（空骨架即可，标题 `福可苏业绩总览 · {页面名}`、更新时间 span `p3Upd`、head 内 `<script>window.PAGE_KEY='{新key}';</script>`）。**新页自带独立 key，与既有页 key 不冲突。**
- **第 2 步 · 接入导航（唯一要动 mapping 的地方）**：`nav.js BOARD_PAGES` 插入新项，并把后续页的 `num`/`name`（P4-P7）与 `id`（文件名）顺延；既有页的 **`key` 一律不动**。
- **第 3 步 · 新页权限**：`users.js pageAccess` 加新 key（如 `"新key": []` 待分配）；**既有页的 key→权限键一律不动**——授权天然不因插入而错位。
- **第 4 步 · 验证**：按 §11.6 清单逐项核对（重点：导航序号与页面名一一对应、各页 js 引用不 404、**新页 key 唯一**、各页授权归属正确、无权限页被重定向）。

### 11.6 验证清单
- [ ] 各页菜单互跳 + 当前页高亮；
- [ ] 数据更新时间 / 时钟 / 天气正常；
- [ ] 全屏按钮可用、关闭恢复；
- [ ] 手机端（≤768px）布局与全屏适配；
- [ ] 左侧悬浮导航栏：各页均有、icon 常驻、hover 展开名称、当前页高亮、桌面端固定不随滚动、高度与内容对齐；
- [ ] 页面身份 key：每页 head 有 `window.PAGE_KEY` 且与 `nav.js BOARD_PAGES` 的 key 一一对应、无重复；
- [ ] 账号权限：administrator 登录见 P0 管理入口、普通用户只见有权限页、无权限页直接访问被重定向；后台管理 P0 的用户管理/页面权限/生成配置可用；插入页面后既有页权限归属不变（keys 不动）。

---

## 12. 后续待跟进

1. **countUp / 无缝滚动去重**：`page2.js` 的 `countUp2` / `setupTblAutoScroll` 与 `js.js` 的 `countUp` / `setupTblScroll` 功能几乎相同（page2 不引 js.js，各自写了一份）。属既有代码 + 跨页公共脚本重构，需小心拆分 js.js 的总览专属逻辑，暂缓。
2. **历史年份目标缺失**：`公司DOM&OB目标` / `挑战目标` 仅 2026 有数据；选 2023-2025 时目标显示 `--`、达成率恒 `--`，无法区分"无数据/无目标"。如需旧年目标需补 Target.xlsx 数据。
3. **患者渠道销量表 Total 口径**：只统计 5 类渠道（国内医生导流 / OB导流 / 香港 / 新加坡 / 沙特），不含无导流的国内直接单，易被误读为总销量——已在 §4.6 注明，保持现状。
4. **手机端新表格体验**：12 月 + YTD 表格在窄屏为横向滚动，是否需在 ≤768px 纵向堆叠 / 简化列，视实际使用反馈再定。
5. **备份清理**：全局 `~/.claude/CLAUDE.md` 已加入"删除/覆盖前先备份"规则；`.bak` 等备份暂定每月清理一次，若总量超 1GB 提醒用户删除（不写自动脚本）。
